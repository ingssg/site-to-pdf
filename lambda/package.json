{
  "name": "site-to-pdf-lambda-worker",
  "version": "1.0.0",
  "description": "AWS Lambda worker for site-to-pdf crawling and PDF generation",
  "main": "index.js",
  "scripts": {
    "deploy": "zip -r function.zip . -x '*.git*' 'node_modules/*' '*.zip'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.89.0",
    "openai": "^6.15.0",
    "playwright": "^1.57.0",
    "pdf-lib": "^1.17.1",
    "jszip": "^3.10.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
