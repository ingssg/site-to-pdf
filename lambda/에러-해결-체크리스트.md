# 에러 해결 체크리스트

## 현재 에러
- **RequestId**: `b15ba4aa-dfe9-4932-8715-b324c8e10395`
- **Timestamp**: `2026-01-11T12:48:42.549Z`
- **에러**: `Executable doesn't exist at /home/sbx_user1051/.cache/ms-playwright/...`

## 해결 단계

### ✅ 1단계: 코드 수정 완료
- [x] Dockerfile에 `PLAYWRIGHT_BROWSERS_PATH` 환경 변수 추가
- [x] crawler.js에 브라우저 경로 자동 감지 로직 추가
- [x] pdf.js에 브라우저 경로 자동 감지 로직 추가

### ⏳ 2단계: Docker 이미지 재빌드 및 배포 (필수!)

#### 2-1. Docker Desktop 실행 확인
```bash
# Docker Desktop이 실행 중인지 확인
docker ps
```

#### 2-2. 이미지 재빌드 및 푸시
```bash
cd lambda
./build-and-push.sh
```

**예상 출력:**
```
✅ 이미지 푸시 완료: [계정ID].dkr.ecr.ap-northeast-2.amazonaws.com/site-to-pdf-lambda-worker:latest
```

#### 2-3. Lambda 함수에 새 이미지 배포
1. AWS Lambda 콘솔 접속
2. `site-to-pdf-worker2` 함수 선택
3. **"Code"** 탭 클릭
4. **"Deploy new image"** 버튼 클릭
5. 이미지 태그 선택: `latest`
6. **"Save"** 클릭

**중요**: 이미지 배포 후 몇 분 정도 기다려야 새 이미지가 적용됩니다.

### ⏳ 3단계: 테스트 및 로그 확인

#### 3-1. 테스트 실행
- Vercel 사이트에서 크롤링 시작
- 또는 Lambda 콘솔에서 테스트 이벤트 실행

#### 3-2. CloudWatch 로그 확인
Lambda 콘솔 → **"모니터링"** → **"CloudWatch에서 로그 보기"** 클릭

**확인할 로그:**
```
[Crawler] Playwright 브라우저 경로: /var/task/.cache/ms-playwright/chromium_headless_shell-1200/...
```

**성공 시:**
- 브라우저 경로 로그가 출력됨
- 크롤링이 정상적으로 시작됨

**실패 시:**
- `Executable doesn't exist` 에러가 계속 발생
- `[Crawler] 브라우저 경로 자동 감지 실패` 로그가 출력됨

## 문제 해결

### 문제 1: 이미지 재빌드 후에도 에러 발생
**원인**: Lambda 함수에 새 이미지가 배포되지 않음
**해결**: Lambda 콘솔에서 "Deploy new image" 확인

### 문제 2: 브라우저 경로 로그가 없음
**원인**: `chromium.executablePath()`가 null 반환
**해결**: Dockerfile에서 Playwright 설치 확인

### 문제 3: 여전히 `/home/sbx_user1051/.cache/...` 경로를 찾음
**원인**: 환경 변수가 제대로 설정되지 않음
**해결**: Lambda 함수의 환경 변수에서 `PLAYWRIGHT_BROWSERS_PATH` 확인

## 현재 상태 확인

다음 명령어로 현재 상태를 확인하세요:

```bash
# Docker 이미지 확인
docker images | grep site-to-pdf-lambda-worker

# ECR 이미지 확인
aws ecr describe-images \
  --repository-name site-to-pdf-lambda-worker \
  --region ap-northeast-2 \
  --query 'sort_by(imageDetails,&imagePushedAt)[-1]'
```
